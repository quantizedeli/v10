# ============================================================
# Nuclear Physics AI Project — HPC requirements
# Use this file on supercomputers (SLURM/PBS clusters)
# Avoids packages that fail to build on Python 3.11+
#
# Install:
#   module load python/3.11
#   python -m venv ~/v10_env
#   source ~/v10_env/bin/activate
#   pip install -r requirements-hpc.txt
# ============================================================

# --- Core Scientific Computing ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# --- Machine Learning ---
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
# catboost>=1.2.0   # May fail on some HPC compilers — optional

# --- Deep Learning ---
tensorflow>=2.13.0
# torch>=2.0.0    # Uncomment if CUDA module available

# --- Hyperparameter Optimisation ---
optuna>=3.3.0
# hyperopt>=0.2.7  # Optional — auto-installed by tpot; include if needed

# NOTE: auto-sklearn EXCLUDED — Python 3.11+ build failure (swig/SMAC dependency)
# NOTE: tpot EXCLUDED — pulls auto-sklearn on some platforms
# NOTE: pickle5 EXCLUDED — built-in since Python 3.8

# --- Data Visualisation ---
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0
# kaleido>=0.2.1  # Optional: plotly PNG/PDF export

# --- Excel & Reporting ---
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# --- Progress & Logging ---
tqdm>=4.65.0
colorama>=0.4.6
rich>=13.0.0

# --- File I/O ---
joblib>=1.3.0
h5py>=3.9.0
pyarrow>=12.0.0

# --- Statistical Analysis ---
statsmodels>=0.14.0
pingouin>=0.5.3

# --- Model Interpretability ---
shap>=0.43.0

# --- Utilities ---
python-dateutil>=2.8.2
pytz>=2023.3
psutil>=5.9.0

# --- LaTeX / Template Generation ---
jinja2>=3.1.0

# --- Testing & QA ---
pytest>=7.4.0
pytest-cov>=4.1.0
coverage>=7.3.0
