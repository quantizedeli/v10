# ============================================================
# Nuclear Physics AI Project — requirements.txt
# Python 3.11+, Windows 11, new PC
# Install: pip install -r requirements.txt
# ============================================================

# --- Core Scientific Computing ---
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# --- Machine Learning ---
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
catboost>=1.2.0

# --- Deep Learning (pick one; both optional) ---
tensorflow>=2.13.0
# torch>=2.0.0              # PyTorch — uncomment if needed
# keras is bundled with tensorflow 2.x — do NOT install standalone

# --- Hyperparameter Optimisation ---
optuna>=3.3.0
hyperopt>=0.2.7

# --- AutoML (optional, heavy) ---
# auto-sklearn is Linux-only; skip on Windows
# tpot>=0.12.0              # TPOT — uncomment on Linux/Mac

# --- Data Visualisation ---
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.17.0
kaleido>=0.2.1              # plotly static PNG/PDF export

# --- Excel & Reporting ---
openpyxl>=3.1.0
xlsxwriter>=3.1.0
# python-docx>=0.8.11       # Word export — uncomment if needed

# --- Progress & Logging ---
tqdm>=4.65.0
colorama>=0.4.6
rich>=13.0.0

# --- File I/O ---
joblib>=1.3.0
h5py>=3.9.0
pyarrow>=12.0.0             # fast parquet/feather (pandas 2.x perf)

# --- Statistical Analysis ---
statsmodels>=0.14.0
pingouin>=0.5.3

# --- Model Interpretability ---
shap>=0.43.0
# lime>=0.2.0.1             # uncomment if needed

# --- Utilities ---
python-dateutil>=2.8.2
pytz>=2023.3
psutil>=5.9.0               # memory/CPU monitoring in training loops

# --- LaTeX / Template Generation ---
jinja2>=3.1.0

# --- Testing & QA ---
pytest>=7.4.0
pytest-cov>=4.1.0
coverage>=7.3.0

# --- MATLAB Integration (optional) ---
# matlab-engine             # Uncomment & install from MATLAB: cd <matlabroot>/extern/engines/python && pip install .

# --- GPU Support (optional) ---
# tensorflow[and-cuda]      # TF + CUDA all-in-one (TF 2.13+)
# cupy-cuda12x>=12.0.0      # CUDA 12.x array ops

# ============================================================
# Quick-install notes for a fresh Windows PC:
#
#   1. Create venv:
#      python -m venv venv && venv\Scripts\activate
#
#   2. Upgrade pip:
#      python -m pip install --upgrade pip
#
#   3. Install requirements:
#      pip install -r requirements.txt
#
#   4. Verify:
#      python -c "import sklearn, xgboost, lightgbm, catboost, optuna, plotly; print('OK')"
#
#   5. TensorFlow GPU (NVIDIA):
#      pip install tensorflow[and-cuda]
#      (requires CUDA 12.x + cuDNN 8.x on the host)
# ============================================================
